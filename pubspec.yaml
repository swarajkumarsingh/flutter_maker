name: flutter_maker
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=3.0.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.0.0
  build_verify: ^3.0.0
  build_version: ^2.0.0

flutter:
